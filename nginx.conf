events {}
rtmp {
    server {
        listen 1935;
        chunk_size 4096;
        application live {
            live on;
            record off;
            push ${YOUTUBE_URL}/${YOUTUBE_KEY};
            push ${FACEBOOK_URL}/${FACEBOOK_KEY};
            on_publish http://127.0.0.1:8080/on_publish;
        }
    }
}